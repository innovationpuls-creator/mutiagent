# 成森 · AI 成长报告开发计划

## 目标与范围

在 `/canopy` 手动生成截至当前的累计总结，回答学到了什么、哪里需要巩固、下一步怎么学。首版包含生成、页面内阅读、证据展开、跳转已有课程、重新生成；不包含历史持久化、日期筛选、PDF 导出或自动修改路径。

## 数据口径

- `ChapterProgress.state == passed` 代表实际通关；图谱状态不能当成掌握证据。
- 后端计算通关章节、参与测验章节、作答次数；同一章节多次作答才比较变化。
- 不引用估算的 `focused_hours`、仅已通关章节最高分平均值 `avg_score`。
- `ChapterWeakness.consumed` 不表示掌握；资源质量不用于学生能力评价。

## 需求拆解

- [x] 后端上下文：按登录用户读取画像、路径、进度、测验摘要、薄弱点；限制模型上下文体积。
- [x] 只读工具：测验证据、章节内容、已发布教材证据；只接受本次上下文提供的标识，不写学习数据或知识缺口。
- [x] 生成：使用现有 Worker 模型，按需读取详情，结构化生成概述、优势、巩固项、最多三项建议；校验引用和课程标识。
- [x] API：认证 SSE 入口 `POST /api/branch/canopy/report/stream`，输出真实阶段、完整报告或失败；纳入 OpenAPI 类型生成。
- [x] 前端：图谱标题放生成入口，图谱与里程碑之间展开报告；暖纸色、珊瑚按钮、鼠尾草绿、霞鹜文楷，移动端单列。
- [x] 交互：阶段提示、骨架、减少动态效果、证据展开、课程跳转、重试；重新生成失败保留旧报告，离开或换账号取消请求并清理报告。
- [x] 验证：统计、权限隔离、工具范围、引用校验、无测验、模型失败；前端生成、证据、重试保留、流中断；格式、构建、桌面与移动端检查。
- [x] 文档：API 事件、统计口径、范围与验证结果。

## 实现与验收约定

- SSE 事件为 `report_stage`、`report_completed`、`report_error`，阶段依实际执行推进。
- 时间、统计和来源由后端产生；AI 提供分析文字、引用和已有课程标识。
- 无测验时生成目标与路径回顾，明确表现待验证，不生成能力结论。
- 模型或校验失败可重试，不把模板冒充 AI 成果；旧报告仍可阅读。
- 当前已有未提交修改保持原样，不改教材服务和已有报告产物。

## 开发记录

- 已核对成森页面、数据模型、统计服务、认证路由和 Worker 模型入口。
- 已实现并验证。接口说明：`docs/api-specs/API-成长报告.md`。
- 后端：`uv run pytest -q tests/test_growth_report.py tests/test_canopy_api.py`，14 项通过；新增文件和路由 Ruff 检查、格式化通过。
- 前端：`npm test`，48 个文件、261 项通过；最终交互调整后针对性重跑 8 项通过。`npm run gen:api`、`npm run build` 通过。
- 浏览器：`npm run e2e -- growth-report.spec.ts` 通过，检查 1440px 桌面、390px 手机、生成时自动定位、证据、失败保留、重试与课程跳转；已查看两端截图。
- 真实 Worker 模型使用合成学习记录验证：证据选择及报告生成可用。修复了练习建议数字被事实校验误拦的问题；非法证据标识要求模型修正一次，仍失败则不展示报告。对应回归测试已覆盖。
- 数值字面量和引用存在性检查不能保证所有自然语言推论正确；原始证据保留在报告中供用户展开核对。
